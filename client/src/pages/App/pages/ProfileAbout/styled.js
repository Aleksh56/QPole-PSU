import styled from '@emotion/styled';
import { Box } from '@mui/material';
import { Rem } from '@/utils/convertToRem';

export const StyledProfileAboutWrapper = styled(Box)(() => ({
  width: '100%',
  position: 'relative',
}));
