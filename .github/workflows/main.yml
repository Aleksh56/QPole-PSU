name: Deploy Docker Container

on:
  push:
    branches:
      - back-dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: List files
        run: |
          echo "Current directory:"
          pwd
          echo "List of files:"
          ls -la
           echo "List of files /server:"
          ls -la ./server

      - name: Build Docker image
        run: docker build --build-arg SECRET_KEY="django-insecure-kf8bj38wyg@vr)42_=50**uo-8$1s%(c$&l#t4ga=(m+@bm7fs" -t django-image ./server

      - name: Save Docker image
        run: docker save django-image > image.tar

      - name: Copy Docker image to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: 89.111.155.6
          username: root
          port: 22
          source: "image.tar"
          target: "/home"

      - name: Deploy Docker container
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: 89.111.155.6
          username: root
          port: 22
          script: |
            docker load < /home/image.tar
            docker stop django-image || true
            docker rm django-image || true
            docker run --name django-image -d django-image
